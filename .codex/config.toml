# Project-scoped Codex settings are loaded only when this repository is trusted.
[agents]
enabled = true
# Three spawned threads plus the primary thread keeps total concurrency at four.
max_concurrent_threads_per_session = 3
