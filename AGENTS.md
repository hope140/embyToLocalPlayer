# 项目协作规则

## 适用范围

本文件适用于整个仓库。更深目录中的 `AGENTS.md` 可以补充局部规则，但不得放宽这里的任务边界、隔离、审核与发布要求。

本仓库采用“主线程 + `luna_worker` 子代理 + PR Stack”工作流。详细操作、计划模板和派发模板见 `docs/pr-stack-workflow.md`。

## 变更原则

- 先检查真实代码、仓库文档、Git 状态和现有改动，再设计或修改。
- 只做当前任务要求的最小变更；保留并绕开用户已有改动。
- 未经用户明确授权，不得 push、创建 PR、合并 PR、强制更新分支或执行不可逆清理。
- 不得泄露密钥、令牌、Cookie、个人信息或不必要的本机路径。
- 子代理报告只是线索，不是验收证据；主线程必须检查真实仓库状态和完整 diff。

## 主线程职责

主线程负责需求分析、架构设计、Stack 拆分、依赖与风险判断、文件所有权分配、子代理派发、代码审查、集成回归和最终验收。

复杂需求进入实现前，主线程必须先输出完整 PR Stack 计划。每个 Stack 至少写明：

- Stack 编号和名称
- 目标
- 基础分支
- 前置依赖
- 允许修改的文件或目录
- 禁止修改的文件或目录
- 文件所有权
- 实现边界
- 验收标准
- 测试命令
- 回滚方式
- 是否允许并行
- 计划使用的子代理

主线程应把边界明确、可验证、可回滚的编码任务交给 `luna_worker`。架构取舍、需求澄清、跨 Stack 集成、最终发布和无法明确划界的修改仍由主线程负责。

## PR Stack 与并发规则

- 无依赖的 Stack 可以从共同基础分支创建。
- 有依赖的 Stack 必须基于直接前置 Stack 的已审核 commit 创建。
- 修改相同文件、共享生成文件、共享迁移或存在接口依赖的 Stack 必须串行。
- 只有文件所有权不重叠、没有依赖且验证不会争用同一可变资源的 Stack 才允许并行。
- 依赖或文件所有权在执行中发生变化时，暂停相关 Stack，由主线程重新规划，不得由子代理自行协调扩大范围。
- 一个 Stack 原则上对应一个分支、一个独立 worktree、一个执行代理线程和一个可独立审核的 commit 序列。

## 子代理与 Worktree 门禁

- 每个执行写操作的子任务必须使用独立代理线程、独立分支和独立 Git worktree。
- 派发前，主线程必须实际运行 `git worktree list`，确认目标 worktree、分支和基础 commit；不得只在提示或报告中声称已隔离。
- 如果运行环境不能为子代理保证独立 worktree，主线程必须先显式执行 `git worktree add` 并把绝对路径交给代理；无法隔离时，多个写任务必须串行，且同一时刻只允许一个写代理使用共享目录。
- 默认最多并发 3 个子代理线程；主线程应按依赖、文件所有权和机器资源进一步降低并发。
- 子代理必须在收到 `docs/pr-stack-workflow.md` 派发契约中的全部字段后才能写入。字段缺失、需求不清、出现文件冲突或发现架构问题时，停止修改并报告主线程。
- 修正任务优先发送回原代理线程，并继续使用原分支和原 worktree；不得无故新建修复代理。

## 主线程审核门禁

子代理完成后，主线程必须在该代理的真实 worktree 中执行并检查：

```bash
git worktree list
git branch --show-current
git status --short
git log --oneline --decorate -10
git diff --stat <基础分支>...HEAD
git diff <基础分支>...HEAD
```

主线程还必须确认：

- 没有修改允许范围之外的文件；
- 没有遗漏未跟踪文件；
- 没有未经批准的依赖或生成文件；
- 没有无关重构；
- 所有验收标准均有代码或测试证据；
- 已按风险重新运行必要的测试、构建、Lint、类型检查或静态检查。

发现问题时，主线程向原代理发送明确、具体、可验证的修正要求。审核通过后，仅由主线程整理最终提交；push、创建 PR 或合并仍需用户明确授权。

## 最终集成与回归

全部 Stack 完成后，主线程必须在最终集成状态运行完整回归，检查 Stack 间接口和行为，并汇总：实际修改文件、测试与构建证据、分支/commit/PR 状态、未执行验证及原因、残余风险。只有全部验收条件满足后才能结束任务。
